export type Site = {
    TITLE: string;
    DESCRIPTION: string;
    AUTHOR: string;
    URL: string;
}; 

export type Route = {
    URL: string;
    TITLE: string;
    ICON: string;
    DESCRIPTION?: string;
}